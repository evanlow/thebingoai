fastapi==0.109.0
# uvicorn >=0.34 ships the sansio WebSocket impl. Required because langsmith
# forces websockets>=15, whose legacy impl dropped `transfer_data_task` — under
# old uvicorn (<0.34) every WebSocket crashed. Do NOT drop below 0.34.
uvicorn[standard]==0.51.0
python-multipart==0.0.6
openai==2.41.1
qdrant-client==1.7.1
tiktoken>=0.7,<1.0
python-dotenv==1.0.0
pydantic[email]>=2.7.4,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
httpx>=0.26.0,<0.28

# LangChain & LangGraph — pinned to last-good resolved set (was unbounded langsmith
# → pip backtracking thrash on PyPI drift; re-pin only after a tested build)
langchain-core==1.4.7
langchain-openai==1.3.2
langchain-anthropic==1.4.6
langchain-ollama==1.1.0
langgraph==1.2.5
langsmith==0.8.15

# Async task processing
celery==5.3.6
redis==5.0.1

# Database
sqlalchemy==2.0.36
alembic==1.13.1
psycopg2-binary==2.9.10
PyMySQL==1.1.0

# Authentication
PyJWT>=2.8.0,<3.0.0
passlib[bcrypt]==1.7.4
bcrypt<4.0.0  # Compatibility with passlib
python-jose[cryptography]==3.3.0
cryptography==49.0.0  # For database password encryption

# Cron scheduling
croniter>=1.3.0,<3.0.0

# File parsing
PyPDF2==3.0.1          # PDF text extraction
python-docx==1.2.0     # DOCX text extraction
Pillow==12.2.0         # Image validation/resize
# pandas is provided by the bingo-csv-connector plugin
# Install the plugin for CSV/Excel dataset support
openpyxl==3.1.5        # Excel (.xlsx) export of chat query results

# Object storage (DigitalOcean Spaces / S3-compatible)
boto3==1.43.29

# DataPlane substrate (Phase 1)
duckdb==1.5.3
pyarrow==24.0.0
google-cloud-storage==2.19.0
google-cloud-bigquery==3.41.0
dlt[parquet]==1.27.2
dbt-core>=1.7,<2.0
dbt-duckdb>=1.7,<2.0
dbt-bigquery>=1.7,<2.0

# SQL parsing and rewriting
sqlglot==30.11.0

# Optional LLM providers
# anthropic==0.18.0
